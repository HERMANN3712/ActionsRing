<Window x:Class="ActionsRing.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Actions Ring"
        Width="450"
        Height="350"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ShowInTaskbar="False"
        Topmost="True"
        KeyDown="Window_KeyDown">

    <Grid Background="Transparent" MouseDown="RootGrid_MouseDown">
        <Canvas x:Name="RingCanvas"/>

        <Border x:Name="CloseButtonBorder"
                Width="70"
                Height="70"
                Background="White"
                CornerRadius="35"
                BorderBrush="#DDDDDD"
                BorderThickness="1"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                MouseLeftButtonUp="CloseButtonBorder_MouseLeftButtonUp">
            <TextBlock Text="X"
                       FontSize="24"
                       FontWeight="Bold"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       TextAlignment="Center"/>
        </Border>
    </Grid>
</Window>